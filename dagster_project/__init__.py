"""Đồ thị asset Dagster cho industry-intel-bot (task 0.12, PRODUCTION_PLAN §7).

Package này SIBLING với `src/`, `dbt_project/` ở gốc repo (đúng cấu trúc §6) — không nằm
trong `src.intel_bot.*`. Asset gọi lại các hàm đã có ở `src.intel_bot.*`, không viết lại
business logic (rào chắn task 0.12).
"""
